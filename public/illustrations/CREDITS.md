# Illustrations Credits

The SVG illustrations used in this application are open-source assets sourced from **unDraw** (https://undraw.co/).

- **License:** MIT / Open Source (unDraw license allows free use in personal and commercial projects without attribution).
- **Modifications:** The SVGs were programmatically recolored to match the app's "Blush & Berry" design system tokens using CSS variables (`var(--color-brand)`, `var(--color-text-secondary)`, etc.).
- **Files Included:**
  - `onboarding-calendar.svg` (originally `calendar.svg`)
  - `onboarding-journal.svg` (originally `diary.svg`)
  - `onboarding-get-started.svg` (originally `hello.svg`)
  - `empty-cycle-history.svg` (originally `no-data.svg`)
  - `empty-vault.svg` (originally `private-data.svg`)
